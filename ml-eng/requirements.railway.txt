# Railway-optimized production dependencies
# Slim image: NO torch / sentence-transformers (dense query embeddings via fastembed ONNX)
# Hybrid retrieval: fastembed for BM25 sparse vectors + Qdrant RRF fusion

# Core BigQuery + data access
google-cloud-bigquery>=3.14.0
google-cloud-storage>=2.14.0
db-dtypes>=1.2.0
pandas>=2.0.0
pyarrow>=14.0.0

# Lightweight numpy (no torch stack)
numpy>=1.24.0,<2.0

# RAG graph + API
# ADZA Confidence Framework (Path B evidence scoring)
acf @ git+https://github.com/open-trace/ACF.git
langgraph>=0.2.0
langchain-core>=0.3.0
langchain>=0.3.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
requests>=2.31.0
PyYAML>=6.0

# Vector DB + hybrid sparse (BM25 via fastembed)
qdrant-client>=1.9.0
fastembed>=0.5.0

# Session storage (in-memory fallback when RAG_REDIS_URL unset)
redis>=5.0.0

# Token counting (optional)
tiktoken>=0.5.0

# Chatbot exports (charts / DOCX / PDF)
matplotlib>=3.7.0
python-docx>=1.1.0
reportlab>=4.0.0

# Streamlit QA UI (Railway streamlit service; unused by API image)
streamlit>=1.28.0

# Optional observability (no-op when LANGFUSE_* keys unset)
langfuse>=3.0
